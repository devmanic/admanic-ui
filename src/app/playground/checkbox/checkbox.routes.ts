import { Routes } from '@angular/router';
import { CheckboxPlaygroundComponent } from './checkbox.component';

export const CHECKBOX_ROUTES: Routes = [
    {path: 'checkbox', component: CheckboxPlaygroundComponent}
];
